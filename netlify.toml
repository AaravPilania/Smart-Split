[build]
  command = "npm run build"
  publish = "dist"

[functions]
  node_bundler = "esbuild"

[functions."keep-alive"]
  schedule = "*/5 * * * *"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
